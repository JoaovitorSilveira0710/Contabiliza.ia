module.exports = [
  {
    ignores: ["node_modules/**"],
    languageOptions: {
      ecmaVersion: 2021,
      sourceType: 'module'
    },
    rules: {
      // Minimal rule set — we rely on .eslintrc.json for developer preferences
    }
  }
]
